# 🔐 دليل النظام الآمن لتحدي الجمعة

## 📋 المحتويات
1. [ملخص التحسينات الأمنية](#ملخص-التحسينات-الأمنية)
2. [معايير قوة كلمة المرور](#معايير-قوة-كلمة-المرور)
3. [آلية التوكنات](#آلية-التوكنات)
4. [حماية الحساب](#حماية-الحساب)
5. [تتبع تاريخ اللعب](#تتبع-تاريخ-اللعب)
6. [إعدادات قاعدة البيانات](#إعدادات-قاعدة-البيانات)

---

## 🎯 ملخص التحسينات الأمنية

### ❌ المشاكل في النظام القديم:
1. **كلمات مرور بدون تشفير** ⚠️
   - تم حفظ كلمات المرور بنصها الصريح
   - لا حماية حتى لو تم اختراق قاعدة البيانات

2. **عدم التحقق من صحة البيانات**
   - لا معايير للبريد الإلكتروني
   - لا متطلبات لقوة كلمة المرور

3. **التوكنات غير الآمنة**
   - حفظ التوكن في localStorage (عرضة لهجمات XSS)
   - لا فترة انتهاء صلاحية واضحة

4. **لا حماية من الهجمات**
   - لا rate limiting على محاولات الدخول
   - لا قفل الحسابات بعد محاولات فاشلة

5. **عدم تتبع الأسئلة**
   - لا معرفة بالأسئلة السابقة
   - عرض نفس الأسئلة للمستخدم

### ✅ التحسينات الجديدة:

#### 1️⃣ **تشفير كلمات المرور (bcrypt)**
```javascript
// قبل:
password: 'password123' ❌

// بعد:
password: '$2b$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcg7b3XeKeUxWdeS...' ✅
```
- استخدام bcrypt مع 10 rounds
- تجزئة لا يمكن فك تشفيرها حتى بقاعدة بيانات مكتشفة

#### 2️⃣ **معايير قوة كلمة المرور**
```
يجب أن تحتوي على:
✅ 8 أحرف على الأقل
✅ أحرف كبيرة (A-Z)
✅ أحرف صغيرة (a-z)
✅ أرقام (0-9)
✅ رموز خاصة (@$!%*?&)

مثال: SecurePass123@
```

#### 3️⃣ **نظام التوكنات الآمن**
```javascript
// Access Token
{
  duration: '1 ساعة',
  type: 'JWT',
  usage: 'تسجيل الدخول والوصول للـ APIs',
  storage: 'localStorage (قريباً: HttpOnly Cookie)',
  expiration: 'إجباري'
}

// Refresh Token
{
  duration: '7 أيام',
  type: 'JWT',
  usage: 'تحديث الـ Access Token عند انتهائه',
  storage: 'localStorage',
  security: 'منفصل عن Access Token'
}
```

#### 4️⃣ **حماية من محاولات الدخول المكررة**
```
المحاولة  | الحالة
--------- | ---------
1-4       | طبيعية
5         | ❌ قفل الحساب لمدة 15 دقيقة
6-10      | قفل ممتد
```

#### 5️⃣ **التحقق من صحة البيانات**
- البريد الإلكتروني: `example@domain.com`
- الاسم: بين 2-50 حرف
- كلمة المرور: معايير قوية (أعلاه)

---

## 🔑 معايير قوة كلمة المرور

### متطلبات صارمة (OWASP Compliant):

```regex
^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$
```

| المعيار | الوصف | مثال |
|--------|--------|--------|
| **الحد الأدنى** | 8 أحرف | ✅ |
| **أحرف كبيرة** | A-Z | Password ✅ |
| **أحرف صغيرة** | a-z | password ✅ |
| **أرقام** | 0-9 | pass123 ✅ |
| **رموز خاصة** | @$!%*?& | Pass@word ✅ |

### أمثلة كلمات مرور:
- ✅ **قوية**: `SecurePass123@` `MyP@ssw0rd!` `Correct#Horse123`
- ❌ **ضعيفة**: `password123` `Pass1234` `MyPassword@`

---

## 🎫 آلية التوكنات

### مسار تسجيل الدخول:
```
1. المستخدم يدخل البريد وكلمة المرور
         ↓
2. التحقق من كلمة المرور (bcrypt.compare)
         ↓
3. إنشاء Access Token (مدته 1 ساعة)
         ↓
4. إنشاء Refresh Token (مدته 7 أيام)
         ↓
5. حفظ التوكنات في localStorage
         ↓
6. إعادة توجيه لصفحة اللعبة
```

### استخدام التوكنات:
```javascript
// كل طلب API يتطلب:
fetch(url, {
  headers: {
    'Authorization': `Bearer ${accessToken}`,
    'Content-Type': 'application/json'
  }
})
```

### تحديث التوكن عند انتهاء صلاحيته:
```javascript
// إذا كان Access Token منتهي الصلاحية:
POST /api/refresh-token
{
  refreshToken: '...'
}

// الرد:
{
  accessToken: '...' (جديد)
}
```

---

## 🛡️ حماية الحساب

### 1️⃣ منع هجمات Brute Force:
```javascript
// بعد 5 محاولات فاشلة:
- قفل الحساب تلقائياً
- مدة القفل: 15 دقيقة
- إرسال رسالة خطأ واضحة للمستخدم
```

### 2️⃣ التحقق من الملكية:
```javascript
// كل مستخدم يمكنه فقط:
- ✅ عرض بيانات حسابه
- ✅ تحديث بياناته الشخصية
- ✅ عرض سجل لعبه

// لا يمكن:
- ❌ عرض بيانات مستخدم آخر
- ❌ تعديل بيانات مستخدم آخر
- ❌ الوصول بدون توكن صحيح
```

### 3️⃣ تسجيل الأنشطة:
```javascript
// يتم تسجيل:
✅ آخر تسجيل دخول
✅ عدد محاولات الدخول الفاشلة
✅ وقت قفل الحساب
```

---

## 📊 تتبع تاريخ اللعب

### الميزات الجديدة:

#### 1️⃣ **تحديد الأسئلة المحلولة**
```javascript
// المستخدم يرى:
const solvedQuestionIds = ['saudi_league-10', 'sports-20', ...];

// عند اختيار فئة جديدة:
- استخدام solvedQuestionIds
- عرض فقط الأسئلة التي لم يحلها
- تحديث القائمة بعد كل جلسة
```

#### 2️⃣ **حفظ جلسة اللعب**
```javascript
{
  date: '2026-02-13',
  team1Name: 'فريق أحمر',
  team2Name: 'فريق أزرق',
  selectedCategories: ['sports', 'history'],
  solvedQuestions: [
    { questionId: 'sports-10', category: 'sports', solvedAt: '2026-02-13T10:30:00Z' },
    { questionId: 'history-20', category: 'history', solvedAt: '2026-02-13T10:45:00Z' }
  ],
  team1Score: 30,
  team2Score: 20,
  winner: 'فريق أحمر'
}
```

#### 3️⃣ **الاستعلام عن الأسئلة الجديدة**
```javascript
// عند التحضير للعبة:
GET /api/solved-questions
{
  solvedQuestionIds: ['sports-10', 'history-20'],
  gameHistory: [...]
}

// ثم تصفية الأسئلة:
const newQuestions = allQuestions.filter(
  q => !solvedQuestionIds.includes(q.id)
)
```

---

## 🗄️ إعدادات قاعدة البيانات

### نموذج المستخدم (User Schema):
```javascript
{
  _id: ObjectId,
  name: String (2-50),
  email: String (unique, validated),
  password: String (hashed, bcrypt),
  phoneNumber: String (optional),
  profilePicture: String (optional),
  
  // مستوى الحزمة (1-3)
  packageLevel: Number (default: 1),
  
  // سجل اللعب
  gameHistory: [{
    date: Date,
    team1Name: String,
    team2Name: String,
    selectedCategories: Array,
    solvedQuestions: [{ questionId, category, solvedAt }],
    team1Score: Number,
    team2Score: Number,
    winner: String
  }],
  
  // الأسئلة المحلولة
  solvedQuestionIds: Set<String>,
  
  // الأمان
  lastLogin: Date,
  loginAttempts: Number,
  lockedUntil: Date,
  
  // الوقت
  createdAt: Date,
  updatedAt: Date
}
```

### مفاتيح الفهرس (Indexes):
```javascript
// للأداء والأمان:
- email (unique, indexed)
- userId (indexed)
- createdAt (indexed)
```

---

## 🚀 متطلبات التشغيل

### المتغيرات البيئية (.env):
```env
# قاعدة البيانات
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/friday-challenge

# الأمان
JWT_SECRET=very-long-random-string-min-32-chars
JWT_REFRESH_SECRET=another-very-long-random-string

# الخادم
PORT=5000
NODE_ENV=production
FRONTEND_URL=https://yourdomain.com

# البريد الإلكتروني (للمستقبل)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=app-password
```

⚠️ **تحذير**: تأكد من تغيير JWT_SECRET و JWT_REFRESH_SECRET في الإنتاج!

---

## 📝 API Endpoints الآمنة

### التسجيل والدخول:
```
POST /api/register
POST /api/login
POST /api/refresh-token
```

### المستخدم:
```
GET  /api/user-package-level/:userId [مطلوب Authorization]
POST /api/increment-package-level [مطلوب Authorization]
PUT  /api/update-profile [مطلوب Authorization]
```

### اللعب:
```
POST /api/save-game-session [مطلوب Authorization]
GET  /api/solved-questions [مطلوب Authorization]
GET  /api/new-questions/:categoryId [مطلوب Authorization]
```

---

## ⚡ خطوات التطبيق

### 1️⃣ تثبيت المكتبات (إذا لم تكن مثبتة):
```bash
npm install bcrypt jsonwebtoken mongoose cors dotenv
```

### 2️⃣ تحديث ملف .env:
```bash
# تحرير backend/.env بالمتغيرات الصحيحة
```

### 3️⃣ تشغيل الخادم:
```bash
cd backend
npm start
# 🚀 Backend running on http://localhost:5000
```

### 4️⃣ تشغيل الواجهة الأمامية:
```bash
cd frontend
npm run dev
# 🚀 Frontend running on http://localhost:5173
```

---

## 🔍 اختبار الأمان

### اختبر تسجيل جديد:
```bash
curl -X POST http://localhost:5000/api/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Ahmed",
    "email": "ahmed@example.com",
    "password": "SecurePass123@",
    "confirmPassword": "SecurePass123@"
  }'
```

### اختبر تسجيل دخول:
```bash
curl -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "emailOrName": "ahmed@example.com",
    "password": "SecurePass123@"
  }'
```

### اختبر API محمي:
```bash
curl -X GET http://localhost:5000/api/solved-questions \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

---

## 📚 المراجع الأمنية

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [bcrypt Documentation](https://www.npmjs.com/package/bcrypt)
- [JWT Best Practices](https://tools.ietf.org/html/rfc8725)
- [Password Security](https://pages.nist.gov/800-63-3/sp800-63b.html)

---

## 🤝 الدعم والمساعدة

إذا واجهت أي مشاكل:
1. تحقق من ملف .env
2. تأكد من اتصال MongoDB
3. تحقق من رسائل الخطأ في Console
4. راجع السجلات (logs) في الخادم

**آخر تحديث**: 13 فبراير 2026
